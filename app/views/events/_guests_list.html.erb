<table class="table">
  <tr>
    <th>
      <%= t 'guests.name' %>
    </th>
    <th>
      <%= t 'guests.email' %>
    </th>
    <th>

    </th>
  </tr>
  <% @event.guests.each do |guest| %>
    <tr>
      <td>
      <%=  guest.name %>
      </td>
      <td>
        <%= guest.email %>
      </td>
      <td>
        <button class="btn btn-default" ><%= t('forms.edit') %></button>
      </td>
    </tr>
  <% end %>
</ul>

